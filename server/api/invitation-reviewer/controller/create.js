const { InvitationReviewer } = require('../model');

/**
 * 
 * 
 * invitation reviewers création:

  titre: /
  abstract: /
  deadline: /
  reviewer: {
    nom: /
    id: /
    mail: ?/
  }

  editor: { // (pour l'instant pas utilisateur)
    mail: /
    journal: /
  }

 * 
 * 
 * 
 */

async function create(req, res, next) {}

module.exports = create;
