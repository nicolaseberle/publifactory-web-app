import actions from './partial-review-actions';
import getters from './partial-review-getters';
import mutations from './partial-review-mutations';

const state = {
	partialReview: []
};

export default {
	state,
	actions,
	getters,
	mutations
};
